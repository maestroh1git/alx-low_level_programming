#include<stdio.h>

/**
 * main - Prints all decimal digits from 0
 *
 * Return: Always(success)
 */

int main(void)
{
	char n;

	for (n = '0'; n <= '9'; n++)
	{
		putchar(n);
	}
	putchar('\n');

return(0)
}
