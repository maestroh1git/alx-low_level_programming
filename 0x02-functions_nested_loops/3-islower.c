#include "main.h"

/**
 * _islower - prints 1 or 0 depending on input
 *
 * @c: test parameter
 *
 * Description: prints all lowercase letters
 * Return: Always(0). return 1 for true 0 for false
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
